pechkin是一个生成pdf的nuget包
但是它会引用五个dll，很大，必须放到bin文件夹下。
所以没有用git提交这几个dll。
直接生成解决方案会还原nuget包，但是这个pechkin不能还原出那五个dll。

所以
【卸载掉这个包，重新搜索安装】，它就会下载这五个dll放到项目根目录，并且复制一份到bin文件夹。
所以根目录的五个dll是没有用的，自己拿出来保存起来就行了，如果bin文件夹中这五个dll缺失再把这五个放进去就行。

如果发布，IIS应用程序池要设置允许32位程序。